mutation GenerateTeamNewPasswords($teamId: ID!, $usersId: [ID!]!, $date: String!) {
  generateTeamNewPasswords(input: { teamId: $teamId, usersId: $usersId, date: $date }) {
    success
    errors {
      field
      messages
    }
    src
  }
}
