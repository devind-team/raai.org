mutation DeleteCompetence($disciplineId: ID!, $competenceId: ID!) {
  deleteCompetence(input: { disciplineId: $disciplineId, competenceId: $competenceId }) {
    success
    errors {
      field
      messages
    }
    id
    __typename
  }
}
